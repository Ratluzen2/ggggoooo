# Ratelozn Services | خدمات راتلوزن 📱

![Version](https://img.shields.io/badge/version-3.3.6-yellow.svg)
![Type](https://img.shields.io/badge/Android-Ready-green)

تطبيق خدمات البطاقات الرقمية والشحن، جاهز للتحويل إلى تطبيق أندرويد (APK) باستخدام Capacitor.

## 📱 خطوات بناء تطبيق الأندرويد (APK)

لقد تم تجهيز ملفات `package.json` و `capacitor.config.ts`. الآن عليك تنفيذ الأوامر التالية في **Terminal** (بعد رفع الملفات إلى جهازك أو GitHub):

### 1. تثبيت الحزم
```bash
npm install
```

### 2. تهيئة بيئة الأندرويد
سيقوم هذا الأمر بإنشاء مجلد `android` الذي يحتوي على ملفات الجافا والجرادل:
```bash
npx cap add android
```

### 3. بناء واجهة الويب ومزامنتها
هذا الأمر يحول كود React إلى ملفات ويضعها داخل مشروع الأندرويد:
```bash
npm run android:build
```
*(ملاحظة: هذا الأمر عبارة عن اختصار قمنا بإضافته يدمج `npm run build` و `npx cap sync`)*

### 4. فتح المشروع واستخراج APK
سيفتح هذا الأمر برنامج **Android Studio** تلقائياً:
```bash
npx cap open android
```
* من داخل Android Studio، اضغط على قائمة `Build` > `Build Bundle(s) / APK(s)` > `Build APK`.

---

## ✨ المميزات الحالية

*   **App ID:** `com.ratelozn.services`
*   **الإصدار:** 3.3.6
*   **طرق الدفع:** فيزا، ماستر كارد فقط.
*   **الوضع:** Dark Mode افتراضي.

## 🛠 التقنيات
*   React + Vite
*   Tailwind CSS
*   Capacitor (لتحويل الويب إلى أندرويد)

---
تم التطوير بواسطة خدمات راتلوزن © 2025